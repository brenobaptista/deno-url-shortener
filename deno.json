{
  "tasks": {
    "start": "deno run --allow-env --allow-net --allow-read --watch app.ts",
    "test": "deno test --allow-env --allow-net --allow-read",
    "coverage": "deno task test --coverage=cov_profile && deno coverage cov_profile"
  },
  "importMap": "./import_map.json"
}
